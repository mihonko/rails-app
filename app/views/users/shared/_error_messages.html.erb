<% if resource.errors.any? %>
  <div id="error_explanation">
    <h3>入力した内容にエラーが<%= resource.errors.count %>件あります</h3>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
